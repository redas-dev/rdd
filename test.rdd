fun test(x: int): int {
    exit(x);
}

test(10);

x: int = 0;

y: int = 10;

if (0){
    exit(1);
}
else if (10 + 1 == 9 + 2){
    from i: int = 0; to i < 19; by i = i + 1; {
        x = x + 1;
    }

    while x < 20 {
        x = x + 1;
        exit(69);
    }

    exit(x);
}
else {
    y = 10 + 1;
    exit(y);
}

exit(12);